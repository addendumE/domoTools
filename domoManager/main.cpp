#include "App.h"

using namespace std;

// rsync -avz -e ssh . pi@192.168.1.113:/mqttRRdb

Logger * logger;
App * app;


int main(int argc, char **argv) {
x
	//logger =new Logger("mqttRecorder");
	//app = new App();
	while(1) {
	    sleep(1);
    }

	return 0;
}
